import React, { Component } from 'react';
import {StyledFooter} from './Style'

export default class Footer extends Component {
    render() { 
        return (  
            <StyledFooter>Created by Anton Lantukh &copy;</StyledFooter>
        );
    }
}
