import React, { Component } from 'react';
import {StyledHeader} from './Style';

export default class Header extends Component {
    render() { 
        return (  
            <StyledHeader>BeActive - To Do</StyledHeader>
        );
    }
}
