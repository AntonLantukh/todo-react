language: node_js

node_js:
  - "9"

cache:
  directories:
    - ~/.npm
    - ~/.cache

install:
  - yarn install

script:
  - yarn build

deploy:
  provider: heroku
  app: todo-react-21
  api_key:
    secure: $HEROKU_KEY